# Splunk Tap for Local Mode

This Tap currently does not have inline junit tests that don't require an external Splunk installation.

The tests rely on ~/.splunkrc to get all connection info and credentials.